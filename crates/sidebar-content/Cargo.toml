[package]
name = "sidebar-content"
version = "0.1.0"
edition = "2021"

[dependencies]
zengeld-chart = { path = "../chart" }
uzor = "=1.0.38"
alerts = { path = "../alerts" }
serde = { version = "1.0", features = ["derive"] }
gate4agent = "=0.1.18"

[dev-dependencies]
serde_json = "1.0"
